Restating the ultimate goal, precisely
Questionnaire → generates a personalized program for any new user (this part already works)
Native iOS + Android apps (not PWA)
Wearable integration — Oura, Garmin (HealthKit/Health Connect + possibly direct API)
Data syncs to a backend you and I (Claude) can both read, so program iteration becomes a data-driven conversation, not a manual copy-paste
That's a real product, not a hobby project anymore. Worth naming that up front.
